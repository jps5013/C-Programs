#include <stdio.h>
#include <cs50.h>
#include <string.h>
#include <ctype.h>
#include <stdlib.h>

int main(int argc, string argv[])
{
    // initiziling command line key at first entry post ./program
    
    int key = atoi(argv[1]);

    /** condition for when user doesn't comply b/c 
     *  count must be 2 if user is compliant
     */
    if(argc != 2 || key < 0)
    {
        printf("You F'd Up\n");
        return 1;
    }
    else
    {
        // printf("Give me your bag\n");
        string plaintext = GetString();
        
       
        for(int i = 0, n = strlen(plaintext); i < n ;i++)
        {
             if(isalpha(plaintext[i]))
             {
                char cipher = (plaintext[i] + key) % 26;
                /* if plainttext[i] + key < 26, you can store 0
                 * items of size 26 in a memory space of size 25
                 * so there is still 25 spaces of memory remaining
                 *so we still increase by 25
                 * if plaintext[i] + key of 26, will wrap around to
                 *remainder value i.e 27 % 26 = 1 + ex p[i] = 'a' or 97, so will return
                 *'b' or 98, as it should
                 */
                printf("%c",cipher);
             }
            else
            {
                printf("%c",plaintext[i]);
                
            }
        }
        printf("\n");    
    }
    return 0; 
}

